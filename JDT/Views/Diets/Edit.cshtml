@model JDT.Models.Diet

@{
    ViewBag.Title = "Edit";
}
<div class="content container">
    <div class="row">
        <div class="col-md-12">
            <h2 class="page-title">Edit your diet<small> Give your workout a name and description.</small></h2>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <section class="widget">
                <header>
                    <h4>
                        <i class="fa fa-align-left"></i>
                        Modify Diet
                    </h4>
                </header>
                <div class="body">
                    @using (Html.BeginForm("Edit", "Diets", FormMethod.Post, new { @class = "form-horizontal", data_validate = "parsley", novalidate = "novalidate", id="Edit" }))
                    {
                        @Html.ValidationSummary(true)
                        <fieldset>
                            <legend class="section">Give your diet a name and description</legend>
                            <div class="row">
                                <div class="col-sm-8">
                                    @Html.HiddenFor(model => model.DietId)
                                    @Html.Partial("_CreateOrEdit", Model)
                                </div>
                            </div>
                        </fieldset>
                        <div class="form-actions">
                            <button type="submit" class="btn btn-danger">Submit</button>
                            <a href="#" id="List">Back to List</a>
                        </div>
                    }
                </div>
            </section>
        </div>
    </div>


</div>
@section scripts{
    @Scripts.Render("~/bundles/jqueryplugins")
    @Scripts.Render("~/bundles/backboneunderscore")
    @Scripts.Render("~/bundles/bootstrap")
    @Scripts.Render("~/bundles/site")
@Scripts.Render("~/Content/js/CrudOps/Diets.js")

<script>
    $(document).ready(function () {
        initEditAuth();
    });

</script>

}